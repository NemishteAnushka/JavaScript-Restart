const cities = ['New York', 'London', 'Paris'];
// Find and log the index of 'London' in the array

const index = cities.indexOf('London');
//we can use indexOf method to fine index in array
console.log("index",index);