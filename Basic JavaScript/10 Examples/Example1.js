// Declare three variables: one for a number, one for a string, and one for a boolean. Perform an operation that involves all three variables and print the result.

let age = 21;
let fname = "Anushka";
let girl = true;

console.log(`My name is ${fname}. I am ${age} year old. ${girl ? "I am girl": "I am not girl"} `);