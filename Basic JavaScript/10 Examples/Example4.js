//Write a program that compares two numbers and prints whether the first number is greater, smaller, or equal to the second number, using the ternary operator.

let num1 = 15;
let num2 = 8;

const compareNum = num1 > num2 ? "first number is greater" : num1 < num2 ?" second number is greater": "Equal numbers";

console.log(compareNum);