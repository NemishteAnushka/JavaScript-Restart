//Write a program that calculates the area of a rectangle. Take the length and width as variables and use the multiplication operator to find the area.
let length = 10;
let breadth = 12;
let area = length * breadth;
console.log("Area of rectangle is " + area);
