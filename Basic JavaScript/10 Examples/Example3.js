//Create a program that checks whether a given number is positive, negative, or zero. Use an if-else statement for this.

let num = 45;

if (num > 0) {
  console.log(`The number ${num} is Positive number`);
} else if (num === 0) {
  console.log(`The number ${num} is nutral number`);
} else {
  console.log(`The number ${num} is negative number`);
}
